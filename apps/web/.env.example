# Required for @ai-sdk/anthropic when invoking the live LLM and form import feature.
ANTHROPIC_API_KEY=

# Optional: override the default model declared in /api/chat/route.ts
# ANTHROPIC_MODEL=claude-3-5-haiku-20241022

# Required for @mendable/firecrawl-js to scrape Google Forms for import feature
FIRECRAWL_API_KEY=
